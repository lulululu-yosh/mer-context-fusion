[project]
name = "mer-context-fusion"
version = "0.1.0"
description = "Context-aware multimodal emotion recognition on MELD"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100